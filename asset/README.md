# Runtime assets

This folder is for image and decorative assets that the live game references directly.

## Imperial seal placeholder

The header currently expects:

`asset/imperial-seal.svg`

That file is intentionally a placeholder. Replace it later with the final Imperial Russian coat of arms asset while keeping the same filename and the UI will pick it up automatically.

The corresponding UI rule lives in `src/asset-placeholders.css`.
